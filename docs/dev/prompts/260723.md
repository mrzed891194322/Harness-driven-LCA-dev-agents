我希望参考‘docs/test/new_dir’，重构一下当前目录，具体如下：

1. 将 GUI 和 scripts 收入 src 目录，注意修改相关路径引用，并在 src 目录下增加 test 目录（主要用于GUI和scripts中py脚本的运行测试，可以只写readme，暂时不实现）。

2. 将 uploads 收入 workspace ，改名为 inputs，其中uploads/plan/execution_plan.md 改为 plan.md ，放在inputs目录下（注意引用）。删除 user_data、user_file ，将其合并为references （内有data、file子目录，文件同步等脚本需注意）。 memory 和 tmp 保留 ，其他全部删除，LCI和results（改名为reports）移动到 outputs 中，注意修改引用

注意全局引用修改（不清楚的可以问我），GUI目录下的暂时不用改（未来改GUI的时候一起全部改），改完测试一下

---

按我的下列需求重构一下 .opencode/agents 、 .opencode/skills/workflow-main/SKILL.md 和 harness/rules：

1. harness/rules/knowledge-retrieval.md 和 harness/rules/openlca-mcp.md 分别建立同名目录，参考同级的harness/rules/coding-specification组织目录结构（如果没有明确的reference子目录，可以只有readme），在.opencode/opencode.json中明确引用 knowledge-retrieval.md，但不引用 harness/rules/openlca-mcp.md ，后者在 .opencode/agents/sub-executor.md 和 .opencode/agents/eval-reviewer.md 的提示词中说明“在需要调用openlca mcp工具时按需读取”

2. 重新组织 agent-skill 的关系：agent的提示词中不应当有任何需要读取具体文件的说明（除了我上一条说的内容），所有需要在工作执行过程中按需读取的文件，请在skill工作流中交代（交代时不能一次性读取所有的文件，而是工作流进行到特定步骤时，调用特定agent，让其按需读取/主agent自行读取某文件），需要注意的是：工作流进行到哪一步再按需读取什么文件，这对于用户理解工作流文本和agent的上下文管理都应当相当重要

---

重构一下src/GUI：

1. 修改其路径到正确位置，使其能正常运行

2. 为其在 src/test 写所需的测试用例，在GUI的readme中明确说明修改代码后需要跑测试

3. GUI中部分按钮对应的函数功能已经更改了，把还能找到引用的重新对上


需要注意的是，其计划面板和LCI制定面板的按钮功能已经被我移除了，所有这部分内容如果重构没法实现可以先放着