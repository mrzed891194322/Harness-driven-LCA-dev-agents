我希望参考‘docs/test/new_dir’，重构一下当前目录，具体如下：

1. 将 GUI 和 scripts 收入 src 目录，注意修改相关路径引用，并在 src 目录下增加 test 目录（主要用于GUI和scripts中py脚本的运行测试，可以只写readme，暂时不实现）。

2. 将 uploads 收入 workspace ，改名为 inputs，其中uploads/plan/execution_plan.md 改为 plan.md ，放在inputs目录下（注意引用）。删除 user_data、user_file ，将其合并为references （内有data、file子目录，文件同步等脚本需注意）。 memory 和 tmp 保留 ，其他全部删除，LCI和results（改名为reports）移动到 outputs 中，注意修改引用

注意全局引用修改（不清楚的可以问我），GUI目录下的暂时不用改（未来改GUI的时候一起全部改），改完测试一下