---
description: 检查 uv、openLCA、可用 Agent CLI，并建议将各 CLI 设为 auto-review。适用于初始化、bootstrap-env 或首次配环境。没有 uv 时判定不通过并请用户手动安装，禁止代装。不要启动 whole-lca。
---

读取并执行 `src/scripts/proj_init/PROMPT.md`。不要改写其中的步骤。不要启动 whole-lca。
