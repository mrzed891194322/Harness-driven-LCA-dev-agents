# 工作流输入

本目录只放工作流结构化输入：

- `plan.md`：whole-lca 的执行计划
- `revise.md`：revise-lca 的用户意见（修订时与既有 `plan.md` 一起使用）

参考资料放到 `harness/knowledge/`，不要放在这里。不使用 GUI 时，先跑 `clean_dir`，再把上述文件放到对应位置，然后在所用 AI 工具中输入 whole-lca / revise-lca 指令。
