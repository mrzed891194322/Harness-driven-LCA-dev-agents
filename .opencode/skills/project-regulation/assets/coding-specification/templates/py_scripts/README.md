# 脚本功能说明 (示例)

本目录下的 Python 脚本用于处理项目中的数据。

## 目录结构
- `main.py`：主入口脚本，仅包含唯一的 `main()` 函数作为总接口。
- `utils/`：存放具体的业务处理辅助函数。
  - `data_helper.py`：包含数据清洗与转换的具体逻辑。

## 维护规范
- **可复用原则**：修改或拓展功能时，应优先修改现有脚本，避免新建同类功能的脚本。
- **说明文档更新**：在修改或新增代码逻辑时，必须阅读并同步更新本 `README.md`，保持文档与代码最新状态一致。
