# LCA 主工作流

Python 主编排读取同名 `LCA-main.yaml`。本文件只给人看。

启动前由 GUI 或用户用 `src/scripts/clean_dir/`（`--preset whole-lca`）清理。计划：`workspace/inputs/plan.md`。资料：`harness/knowledge/`。

循环、委派提示词与 handoff 约定以 YAML 为准。阶段验收以各编号包 README 为准。
