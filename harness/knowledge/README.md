# 用户参考资料

本目录是用户参考资料的**默认落点**：报告、说明、表格、清单等由 GUI 或手工放入此处。不要再建子目录约定，也不要把文件同步到 `workspace/inputs/`。

**不含** `plan.md` 或 `revise.md`；这两类工作流输入固定在 `workspace/inputs/`。

Agent 需要项目事实、数值或清单时，默认直接读取本目录中的文件。任务若绑定了已注册的检索工具，也可使用那些来源。口径见 `harness/rules/lca/knowledge-files.md`。

本目录不存放 ISO 标准全文或 openLCA 手册。
