# 知识库

| 子目录 | 用途 |
| --- | --- |
| `inputs/` | 用户参考资料（报告、表格、清单等） |
| `plan/` | `main_plan.md` 与 `revise_plan.md` |

二者均通过 workflow YAML 的 `knowledge_sources` 注入；口径见 `harness/rules/lca/knowledge-files.md`。

本目录不存放 ISO 标准全文或 openLCA 手册。
