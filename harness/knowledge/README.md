# 用户参考资料

本目录是用户参考资料的**唯一落点**：报告、说明、表格、清单等由 GUI 或手工放入此处。不要再建子目录约定，也不要把文件同步到 `workspace/inputs/`。

Agent 需要项目事实、数值或清单时，直接读取本目录中的文件。LCA 方法要求见 `harness/rules/lca-knowledge/README.md`；openLCA 操作见 `harness/rules/openlca-operation/README.md`。不要调用 `query_rag`。

本目录不存放 ISO 标准全文或 openLCA 手册。
