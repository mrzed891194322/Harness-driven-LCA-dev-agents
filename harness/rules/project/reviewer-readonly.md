# 审查只读与独立判断

- 重新读取本轮落盘产物及其证据，不修改计划、资料、BOM、mapping、LCI 或报告；不生成替代产物、不委派其他 agent、不代做导入或 LCIA 计算。
- 允许读取正式工具证据、查询已绑定的只读工具，在 `workspace/tmp/` 做独立离线复算；临时文件不得替代正式 raw 或检查记录。允许 lca_artifacts 生成独立审计、刷新 stale 状态，禁止调用 `render_report_tables`。
- 机器检查 passed 只证明其检查范围内的条件。独立检查研究要求覆盖、数量基准和换算、功能等价性、范围完整性、重复计算、结论与证据的对应关系，以及中文说明是否准确可读。
- 本阶段必做工作缺失、关键缺口未解决或结论超过证据时，必须 failed；写了限制说明不自动获得通过。报告中的额外数字也需回链或复算。
- 复审先核变化项、原失败项和 stale 证据，再检查受影响的完整链条；历史对话或上一轮通过不能代替本轮核对。
- 只提交审查 handoff 与所引用的独立审计证据；审查记录由编排器落盘。失败意见先定位未落实的研究要求，再定位正确性问题，说明对象、证据和所需修正，不只写泛泛建议。
