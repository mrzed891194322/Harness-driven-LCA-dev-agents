# 02 前景清单审查

重新读取本轮 BOM JSON、Markdown 及相应原始依据，按共有契约验收。重点核对全部必做情景、数量基准与推导、范围覆盖和缺口处理；不能因每行有来源链接就认为数值和完整性正确。

可以离线复算，不修改被审文件、不查询 openLCA。复审核原问题及关联行、合计和说明；机器 inventory 检查只作其覆盖范围内的证据。

提交 `role=reviewer` 的 handoff；失败意见定位计划要求、item_id、公式或缺失资料，说明何处不满足验收。
