# 01 初始化检查（审查）

按共有契约和共同方法规则核对计划、声明资料及已知能力。研究目的和必做情景不能只核标题是否存在；修订任务同时应用本阶段修订补充契约。

按规则从计划措辞**自主分类**每条要求：解释类可后续出处表 + `llm_inferred` 落实，不得因 plan 未写「允许 LLM」或未声明能力档位而 failed；仅硬工具交付且当前无能力时 failed。

不修改输入，不查库、不生成替代计划或业务产物。缺少内部编号不是失败理由。提交 `role=reviewer` 的 handoff，失败意见定位具体要求及缺失事实；本阶段不返工。
