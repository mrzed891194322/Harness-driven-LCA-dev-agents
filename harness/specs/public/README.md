# Whole-LCA 公共运行说明

本目录只说明共用路径与 `manifest.json`。阶段做什么见对应编号包 README；循环见 `harness/workflows/`。

1. `references/workflow-runtime-spec.md`：路径与 `completed` / `failed`
2. `references/templates/checklist.md`：可选人读清单
3. `references/examples/`：薄 `manifest.json` 与审查笔记示例
