# LCA 结果报告

## 1. 目标、范围与功能单位

说明研究对象、目的、系统边界、功能单位和适用限制。

### 研究要求落实情况

对照有效 plan/revise 的原文，覆盖全部必做与可选要求。必做工作缺失时报告其失败原因，不能把“未执行”写成完成。完成情况可由写者标注 `tool_backed` 或 `llm_inferred`（写者义务，非用户 plan 字段）；证据位置可指向出处表行或 raw 路径。

| 要求及原文位置 | 必做/可选 | 情景或分析 | 完成情况 | 证据位置 |
| --- | --- | --- | --- | --- |

## 2. 前景清单

用表格列出 BOM `item_id`、名称、数量、单位、来源。完整清单见 `workspace/outputs/inventory/extracted-bom.json`。

<!-- lca:inventory:start -->
<!-- lca:inventory:end -->

## 3. 数据集映射

用表格列出 `item_id`、选用 Process/Provider 名称与 UUID、请求/实际地域、理由。完整映射见 `workspace/outputs/inventory/process-mapping.json`。

<!-- lca:mapping:start -->
<!-- lca:mapping:end -->

## 4. 模型与数据来源

说明活动数据库、前景过程、分配和参数。

区分用户声明的数据库版本/系统模型与工具已验证的信息。说明数据时间、技术和地域代表性、前景聚合的实际负荷、计划允许排除的项目及避免重复计算的依据；未知如实标记。

### 数量与计算基准

| 情景 / Product System | 功能单位 | 参考流与单位 | 定量参考输出 | 实际计算 amount | 换算依据与 raw |
| --- | --- | --- | --- | --- | --- |

说明源数据到 BOM、exchange、参考流及实际 amount 的对应关系，引用已审换算说明及本轮 calculation-plan/raw，确保下列结果以所述功能单位为分母。

## 5. LCIA 方法与结果

列出 Product System、方法名称/UUID，以及影响类别、数值、单位和原始结果位置。

<!-- lca:lcia:start -->
<!-- lca:lcia:end -->

### 情景比较与敏感性分析

逐项回答研究问题，核对功能等价、功能单位、范围、方法和设置，说明各情景改变的因素。提供计划要求的敏感性结果、差异计算依据和 raw 引用；不跨单位汇总影响类别，不凭单项结果声称总体优势。

## 6. 解释、限制与未解决项

对照目标说明结果支持的发现、完整性和一致性，讨论主要假设、数据质量、地域代理和未解决项如何限制结论。敏感性或稳健性判断须有相应证据，未做分析不能宣称不影响排序。

过程贡献、热点与改善归因须登记在出处表：有正式贡献分解或等价工具 raw 时标 `tool_backed`；当前无分解工具时由写者按规则标 `llm_inferred`，依据可引用 BOM、mapping、LCIA 类别总量等，不得写成 openLCA 过程贡献分解或无依据的贡献百分比。必做硬计算缺失须明确未完成，不能靠本节限制说明获得通过。给出与证据相称的结论和建议，区分计划允许排除与仍未解决的数据缺口。

### 出处表

关键解释主张（贡献/热点/改善归因，以及超出 LCIA 生成表字面值的推断）须逐行登记。`provenance` 仅允许 `tool_backed` 或 `llm_inferred`；依据路径为 workspace 相对路径，可多项、分号分隔。

| 主张 | provenance | 依据路径 | 局限 |
| --- | --- | --- | --- |

## 7. 声明边界

本报告不自动构成 ISO 认证、关键审查结论或面向公众的比较优势声明。
