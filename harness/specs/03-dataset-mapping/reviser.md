# 03 背景映射与 LCI 修订（执行）

读取本轮已审 BOM、plan/revise 及上一轮 mapping/LCI，按共有和修订契约提交完整新版本，不覆盖 plan。

受修订影响的已有背景实体重新正式核查；未改变且仍有效的前景实体保持 UUID，新建前景实体按映射规则创建。同步所有受影响的情景、数量换算、关联实体和说明，不留下与现行要求矛盾的旧数据。

定稿后刷新 Provider 验证。返工先解决修订要求缺口，再修正确性及关联项；本阶段不导入。提交 `role=reviser` 的 handoff 和完整产物，不只交差量。
